# TODO make things more strict, like forbidding unsafe

[package]
name = "lattice-psbt"
version = "0.1.0"
edition = "2024"

[features]
_internal = []

[dependencies]
bitcoin = "0.32.2"
thiserror = "2.0.17"
psbt-v2 = "0.3.0"

[dev-dependencies]
proptest = "1"
