[package]
name = "concurrent-psbt-fuzz-support"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
arbitrary = { version = "1", features = ["derive"] }
bitcoin = "0.32"
concurrent-psbt = { path = ".." }
psbt-v2 = "0.3.0"

[workspace]
members = ["."]
