[package]
name = "concurrent-psbt"
version.workspace = true
edition = "2024"

[features]
default = ["unit-tests", "prop-tests"]
unit-tests = []
prop-tests = []

[dev-dependencies]
tracing = "0.1"
tracing-chrome = "0.7"
tracing-subscriber = "0.3"

[lints]
workspace = true
